\section{Анализ экстремальной задачи}
\label{sec:optimality}

Для доказательства разрешимости задачи \eqref{minimization_operational} нам необходимо также установить некоторые свойства решения задачи \eqref{initial}, \eqref{initial_boundary}.

\begin{lemma}[\cite{lemma_proof}]

\label{SolvabilityLemma}

    Если условие (i) выполняются, то для каждого $ u \in U_{ad} $ существует единственное слабое решение $\{\theta, \varphi \}$ для задачи \eqref{initial},\eqref{initial_boundary} и верны следующие оценки:
    \begin{equation}
        \label{lemma_1}
     M_1 \le \theta \le M_2, \; M_1^4 \le \varphi \le M_2^4,
     \end{equation}

    \begin{equation}
    \label{lemma_2}
     \| \nabla \varphi \|^2 \le C.
     \end{equation}
    Здесь $M_1 = \text{ess inf } \theta_b, M_2 = \text{ess sup } \theta_b$, и константа $C > 0$ зависит только от \\ $a, b, \alpha, \kappa_a, \|u\|_{L^\infty(\Gamma)}, \| \gamma \|_{L^\infty(\Gamma)}$ и области $\Omega$.
\end{lemma}

На основе оценок \eqref{lemma_1} и \eqref{lemma_2} доказывается разрешимость экстремальной задачи \eqref{minimization_operational}.
\begin{theorem}[см. \cite{cheb_origin}]
    Если условие (i) выполняется, тогда существует хотя бы одно решение задачи \eqref{minimization_operational}.
\end{theorem}


Для вывода системы оптимальности, покажем дифференцируемость функционала $J$.
\begin{lemma}
\label{freshet_diff}
    Функционал $J : V \rightarrow \mathbb{R}$ дифференцируем по Фреше.
\end{lemma}

\begin{proof}
Покажем, что для произвольной функции $\theta \in V$ выполняется следующее равенство:

\begin{equation}
\label{lemma_proof_1}
J(\theta + h) = J(\theta) + J'(\theta)\langle h \rangle + r(\theta, h) \; \forall h \in V, \; \text{ где } \; J'(\theta)\langle h \rangle = \int_{\Gamma_2} (\theta - \theta_0)h d\Gamma.
\end{equation}

Причем для остаточного члена $r(\theta,h)$ должно быть верно соотношение:
\begin{equation}
\label{lemma_proof_2} \frac{1}{2} \frac{|r(\theta,h)|}{\|h\|_V} \rightarrow 0 \quad \text{при} \quad \|h\|_V \rightarrow 0.
\end{equation}


Перепишем \eqref{lemma_proof_1} в виде

$$
    \frac{1}{2} \|\theta + h - \theta_0\|^2_{L^2(\Gamma_2)} =
    \frac{1}{2} \| \theta - \theta_0 \|^2_{L^2(\Gamma_2)} +
    (\theta - \theta_0, h)_{L^2(\Gamma_2)} +
    \frac{1}{2}\| h \|^2_{L^2(\Gamma_2)}.
$$



Согласно теореме о следах \cite{alekseev_monograph} $ \|h\|_{L^2(\Gamma_2)} \le C \|h\|_V $, где $C$ не зависит от $h$. Поэтому
$$
\frac{1}{2}
\frac{r(\theta,h)}{\| h \|_V} \leq
\frac{1}{2} C^2 \| h \|_V \rightarrow 0 \quad \text{при } \| h \|_V \rightarrow 0
$$

\end{proof}

Вывод условий оптимальности основан на принципе множителей Лагранжа для гладких выпуклых задач минимизации.
\begin{theorem}
\label{adjoint_theorem}
    Пусть $\hat{y}=\{\hat{\theta},\hat{\varphi} \} \in Y, \hat{u} \in U_{ad}$ решение  экстремальной задачи \eqref{minimization_operational}. Тогда существует пара $p = (p_1, p_2), p \in \mathbb{R}_{+} \times Y \setminus \{0\}$
    такая, что тройка $(\hat{y}, \hat{u}, p)$, удовлетворяет следующим условиям:
    \begin{equation}
        \label{therorem_2_eq1}
         A_1 p_1 + 4 |\hat{\theta}|^3 \kappa_a(b p_1 - p_2) = f_c, \;\; (f_c,v) = - \int_{\Gamma_2} (\hat{\theta} - \theta_0) v d\Gamma,
    \end{equation}

    \begin{equation}
        \label{therorem_2_eq2}
        A_2 p_2 + \kappa_a (p_2-b p_1) = g_c(( p_2, \hat{u}),v), \;\; g_c(( p_2, \hat{u}),v) = -\int_{\Gamma_1} \hat{u} p_2 v\Gamma,
    \end{equation}

    \begin{equation}
        \label{therorem_2_eq3}
        \int_{\Gamma_1} p_2 (\hat{\varphi} - \theta_b^4)(u-w) \leq 0 \quad \forall w \in U_{ad}.
    \end{equation}
\end{theorem}
\begin{proof}
    Перепишем уравнения \eqref{weak_operational} следующим образом:
    $$H(y,u) = 0,\;\; y = \{\theta,\varphi\} \in Y,$$
    где
    $$ H:Y \times U \to Y', $$
    $$H(y,u) =\{A_1 \theta + b \kappa_a (| \theta | \theta^3 - \varphi ) - f, A_2 \varphi + \kappa_a (\varphi - |\theta|\theta^3) + F(\varphi, u) - g \}.$$
    Заметим, что для всех $u \in U_{ad}$, отображение $y \to J(\theta) $ и $y \to H(y,u)$ непрерывно дифференцируемо в окрестности $\mathcal{O}(\hat{y})$ точки $\hat{y}$. Непрерывная дифференцируемость членов в $H$ следует из непрерывной дифференцируемости функции $t \in \mathbb{R} \to | t | t^3$, а также из непрерывности вложения  $V \subset L^6(\Omega)$.
    В дополнение, отображение $u \to H(y,u)$ непрерывно из $U \to Y'$ и афинно.

    В статье \cite{theorem_2_proof} показано, что $\text{Im}H_y'(\hat{y}, \hat{u}) = Y$, что влечёт невырожденность условий оптимальности.
    а также условие $\text{codim}(\text{Im}H_y') < \infty$.

    Рассмотрим функцию Лагранжа
    $L(y,u,p) = J(\theta) + (H(y,u),p),$
    где $y,p \in Y,\, u \in U_{ad}$. Согласно принципу Лагранжа \cite[Гл.2, Теорема 1.5]{theorem_proof_18} существует пара $p = \{p_1,p_2\} \in Y$ такая, что

    \begin{equation}
        \label{th2_proof_1}
        (L_\theta,\zeta) =\int_{\Gamma_2}(\hat\theta -\theta_0) \zeta d\Gamma + (A_1 \zeta + 4b\kappa_a |\hat\theta|^3 \zeta,p_1) - 4\kappa_a(|\hat\theta|^3 \zeta,p_2) = 0, \; \forall \zeta \in V,
    \end{equation}

    \begin{equation}
        \label{th2_proof_2}
        (L_\varphi, \zeta) = (A_2 \zeta + \kappa_a \zeta, p_2) - b \kappa_a(\zeta,p_1) +\int_{\Gamma_1} \hat u \zeta p_2 = 0, \; \forall \zeta \in V,
    \end{equation}

    \begin{equation}
        \label{th2_proof_3}
        (L_u,\tau) = \int_{\Gamma_1} \tau (\varphi - \theta^4_b) p_2 d\Gamma  \leq 0, \; \tau := \hat u - w, \forall w \in U_{ad}.
    \end{equation}

    Сопряженные уравнения являются прямым следствием \eqref{th2_proof_1} и \eqref{th2_proof_2}.

\end{proof}

% Чеботарев: Дальше не нужно, так как ImH'_y =  Y'

% Для доказательства следующей теоремы, нам потребуется лемма
% \begin{lemma}[Лемма 2 в \cite{covt_last}]
%     Пусть $h > 0$ некоторая функция из $L^\infty(\Omega)$. Если функции $q_1,q_2 \in V$ удовлетворяют уравнениям
%     \begin{equation}
%     \label{lemma_eq_for_theorem}
%         A_1q_1 +b \kappa_a(hq_1 - q_2) = 0, A2q_2 + F(q_2,\hat{u}) + \kappa_a(q_2 - h q_1) = 0,
%     \end{equation}
%     тогда $q_1 = q_2 = 0$.
% \end{lemma}

% Теперь, ссылаясь на приведенную лемму мы можем доказать следующую теорему
% \begin{theorem}[Теорема 3 в \cite{covt_last}]

%     Пусть $\hat{y} = \{\hat{\theta}, \hat{\varphi}\} \in Y$, $\hat{u} \in U_{ad}$ будет решением зачади оптимального управления \eqref{minimization_operational}. Тогда существует пара $p = \{p_1,p_2\} \in Y$ такая, что тройка значений $(y, u, p)$ удовлетворяет системе уравнений
%     \begin{equation}
%         A_1 p_1 + 4 \hat{\theta}^3 \kappa_a(b p_1 - p_2) = f_c, \;\;
%         f_c = - (\theta - \theta_0) \; \text{ на } \; \Gamma_2,
%     \end{equation}

%     \begin{equation}
%         A_2 p_2 + \kappa_a (p_2-b p_1) = g_c( p_2, \hat{u}),
%         \;\; g_c(p_2,\hat{u}) = - u p_2 \; \text{ на } \; \Gamma_1,
%     \end{equation}

%     \begin{equation}
%         \int_{\Gamma_1} p_2 (\hat{\varphi} - \theta_b^4)(w - u) d\Gamma \ge 0, \; \; \forall w \in U_{ad}.
%     \end{equation}
% \end{theorem}
% \begin{proof}
% Предположим, что множитель $\lambda$ равен нулю для функционалов $f_c$ и $g_c(p_2,\hat{u})$ в уравнениях \eqref{therorem_2_eq1} и \eqref{therorem_2_eq2}. Покажем далее, из этого предположения следует равенство $p = 0$, что, в свою очередь, противоречит теореме  \ref{adjoint_theorem} и, тем самым, позволяет установить невырожденность $\lambda$ и позволяет положить множитель $\lambda$ равным $1$.

% Для этого перепишем уравения \eqref{therorem_2_eq1} и \eqref{therorem_2_eq2} в операторном виде в пространстве $Y = V \times V$, со калярным произведением:
% $$
%     (y,z)_Y = (A_1,y_1,z_1) + (A_2 y_2 + F(y_2, \hat{u}), z_2), \;\; y=\{y_1,y_2\}, z = \{z_1,z_2\}
% $$
% Определим оператор $R: Y \rightarrow Y$ как
% $$
%     Rp = \{A_1^{-1} (4 \hat{\theta}^3 \kappa_a (b p_1 - p_2)), \kappa_a A^{-1}_2(p_2 -b p_1) \},
% $$
% тогда система уравнений \eqref{therorem_2_eq1} и \eqref{therorem_2_eq2} при $\lambda = 0$ эквивалентна уравнению
% \begin{equation}
% \label{fredholm_type}
%     p + Rp = 0 \, \text{в} \; Y
% \end{equation}
% Покажем, что оператор $R$ является компактным. Действительно, следующее равенство верно для всех $\nu =\{\nu_1, \nu_2\} \in Y$
% $$
%     (R_p,\nu)_Y = (4 \hat{\theta}^3 \kappa_a (b p_1 - p_2), \nu_1) + \kappa_a(p_2 - b p_1, \nu_2) \leq
%     C(\| p_1 \| + \| p_2 \|) \| \nu \|_Y,
% $$
% где константа $C > 0$ не зависит от $p$ и $\nu$.
% Таким образом, если мы положим $\nu = Rp$ мы получим следующую оценку
% $$
%     \|Rp\|_Y \le C(\|p_1\|+\|p_2\|).
% $$
% Компактность оператора $R$ следует из компактности вложения $V \subset H$. Таким образом однозначная разрешимость уравнения Фредгольма \eqref{fredholm_type} следует из условия $Ker(I+R*) = \{0\}$. Или, другими словами, уравнение $q + R*q =0$ должно иметь только вырожденное решение. Отметим теперь, что последнее уравнение тождественно системе уравнений \eqref{lemma_eq_for_theorem}
%     % TODO: МОжно ли просто сослаться на [lemma 2]{covt_last}
% \end{proof}
